name: cellectpy3 # USED FOR EVERYTHING PYTHON EXCEPT LDSC.PY
channels:
  - anaconda
  - defaults
  - conda-forge
  - bioconda
dependencies:
  - python=3.6
  - pybedtools=0.8 # has bedtools (and htslib?) as dependency
  - pandas=0.24.2
  - numpy=1.16.3
  - bedops=2.4.37

